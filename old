	  <?php
	  foreach (json_decode($json) as $data) {
	    echo '<h2>'.$data->title.'</h2>';
	    echo $data->date.'<br>';
	    echo Markdown($data->content);
		echo ("<hr>");
	  }
	  ?>

<hr>

<?php //print_r($newArray); ?>


<hr>

<?php

$row = 1;
$mycsvfile = array(); //define the main array.
if (($handle = fopen($feed, "r")) !== FALSE) {
    while (($data = fgetcsv($handle, 1000, ",")) !== FALSE) {
        $num = count($data);
        echo "<p> $num fields in line $row: <br /></p>\n";
        $row++;
        for ($c=0; $c < $num; $c++) {
            echo $data[$c] . "<br />\n";
        }
        $mycsvfile[] = $data; //add the row to the main array.
    }
    fclose($handle);
}

echo '<hr>'.$mycsvfile[1][1]; //prints the 4th row, second column.

?>

<hr>


<?php print_r($mycsvfile); ?>


<hr>